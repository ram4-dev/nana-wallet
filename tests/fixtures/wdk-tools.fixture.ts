// Canonical implementation lives in src/agent/wdk-tools.fixture.ts because
// src/agent/wdk-tools.ts also needs it at runtime for WDK_TOOLS_SOURCE=fixture.
export * from '../../src/agent/wdk-tools.fixture.js';
