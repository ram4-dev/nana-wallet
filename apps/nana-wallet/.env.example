VITE_API_URL=http://localhost:3000
VITE_AGENT_BACKEND=1
# Development-only LiveKit room access. The token must publish as DEMO_USER_ID.
VITE_LIVEKIT_URL=
VITE_LIVEKIT_TOKEN=
VITE_LIVEKIT_AGENT_IDENTITY=nani-agent
# Development-only room access. Never place API secrets or binding private keys in VITE_*.
# The token must be scoped to the single DEMO_USER_ID and a short-lived room.
